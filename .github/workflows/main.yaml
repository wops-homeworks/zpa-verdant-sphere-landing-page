name: "Main Workflow"

on:
  pull_request:
    branches:
      - dev
      - main

jobs:
  build:
    name: Build The Project
    uses: ./.github/workflows/build.yaml
    with:
      node_version: 18

  upload:
    name: Upload To S3
    needs: [build]
    uses: ./.github/workflows/upload.yaml